<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @review.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @review.description %>
</p>

<% if user_signed_in? %>

          <% 
          
          user = User.find(@review.user_id)

            if current_user.id == user.id %>

			<%= link_to 'Edit', edit_review_path(@review) %> 


			|
			<%end%>  


        <%end%>  
<%= link_to ' Back', reviews_path %>

<br>
<br>


<h3>Comment (<%= @review.comments.size %>)  </h3>

	<div id="comment_class">	
		<%= render partial: 'reviews/comment', collection: @review.comments %>
	</div>

	<br>
	<%if user_signed_in? %>

		<%= render partial: 'comments/form' %><br>

	<%else%>
		
	<%=link_to 'Do you have any comments ?', new_user_session_path %>

		<br>
	<%end%>

